FROM rust:1.76 AS builder

WORKDIR /spacey
COPY spacey .
# Cargo.lock is ignored by the repo, but if it is generated by the host Rust in
# the Corpus, then it can cause problems.
RUN rm -f Cargo.lock && \
    cargo test && \
    RUSTFLAGS='-C target-feature=+crt-static' cargo build --release --target x86_64-unknown-linux-gnu

FROM scratch

COPY --from=builder /spacey/target/x86_64-unknown-linux-gnu/release/spacey /
COPY spacey/resources/ws/*.ws /
ENTRYPOINT ["/spacey"]
